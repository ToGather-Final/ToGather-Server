server:
  port: 8080
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://database-mysql.c5qikmwa4vnu.ap-southeast-2.rds.amazonaws.com
    username: admin
    password: togather1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  data:
    redis:
      host:
      port:
      password:

jwt:
  issuer: togather
  access-exp-seconds: 1800
  refresh-exp-days: 7
  secret: ${JWT_SECRET:change-this-secret-to-256bit-string}