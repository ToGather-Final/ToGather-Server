server:
  port: 8080

spring:
  application:
    name: trading-service
  profiles:
    include: common
  data:
    redis:
      host: ${app.redis.host}
      port: ${app.redis.port}
      username: ${app.redis.username}
      password: ${app.redis.password}