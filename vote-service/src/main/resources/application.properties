spring.application.name=vote-service
server.port=8080

# Actuator 설정
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always

# 로깅 설정
logging.level.com.example.vote_service=INFO
