apiVersion: v1
kind: Secret
metadata:
  name: togather-secret
  namespace: togather
type: Opaque
data:
  # 이 값들은 GitHub Actions에서 실제 값으로 교체됩니다
  DB_PASSWORD: PLACEHOLDER_DB_PASSWORD
  JWT_SECRET_KEY: PLACEHOLDER_JWT_SECRET_KEY
  RABBITMQ_PASSWORD: PLACEHOLDER_RABBITMQ_PASSWORD
