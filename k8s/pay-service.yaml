apiVersion: apps/v1
kind: Deployment
metadata:
  name: pay-service
  namespace: togather
  labels:
    app: pay-service
spec:
  replicas: 1  # 🚀 3-Node 환경: 최소 1개, HPA로 스케일링
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1         # 일시적으로 +1개 허용 (총 2개)
      maxUnavailable: 0   # 최소 1개 유지 (가용성 보장)
  selector:
    matchLabels:
      app: pay-service
  template:
    metadata:
      labels:
        app: pay-service
    spec:
      containers:
      - name: pay-service
        image: ${ECR_REGISTRY}/togather/pay-service:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "main"
        - name: LOGGING_LEVEL_ROOT
          value: "WARN"
        - name: LOGGING_LEVEL_COM_EXAMPLE
          value: "INFO"
        - name: LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_SECURITY
          value: "INFO"
        envFrom:
          - configMapRef:
              name: togather-config
          - configMapRef:
              name: togather-db-config
          - secretRef:
              name: togather-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"  # 🚀 CPU 요청 감소
          limits:
            memory: "512Mi"
            cpu: "200m"  # 🚀 CPU 제한 감소
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 120
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 90
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: pay-service
  namespace: togather
spec:
  selector:
    app: pay-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
